import {
    TextField,
    Button,
    Paper,
    List,
} from "@mui/material";
import PropTypes from "prop-types";
import React, { useState } from "react";
const TextField_Activity = (props) => {
    const {}
    return 
    <>
    </>
    TextField_Activity.propTypes = {
    props.search: PropTypes.any.isRequired,
    props.onChange:PropTypes.func.isRequired
    }
}
const ExerciseComponent = (props) => {
    const [calory, setCalory] = useState("");
    const onChange = (e) => {
        setCalory(e.target.value);
    }
    let default_w = 55;
    let default_d = 15;
    return 
    <>
    
    </>
}
export default ExerciseComponent;